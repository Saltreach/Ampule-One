sentence-transformers
llama-cpp-python
numpy
requests
beautifulsoup4
# faiss-cpu is optional — install separately if available for your platform.
# If it is not installed, Project Ampule falls back to NumPy cosine retrieval.
# pip install faiss-cpu

# libzim is required to read Kiwix ZIM files downloaded via download_kiwix.py.
# If not installed, ZIM ingestion is skipped and the Wikipedia API fallback is used.
# pip install libzim
